const chai = require("chai");
const should = chai.should();
const assert = require("assert");

describe("Environment", function() {
  it("should have tests", function() {});
});

describe('JS models', function() {
  describe('calculate function', function() {
    it('should print a string number', funciton() {});
    it('should only accept an array', function(){});
    it('should make the proper calculation', function() {});
    it('should have chainable calculations', function() {});
  })
})
